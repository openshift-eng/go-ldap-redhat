# LDAP Red Hat - Configuration Example
# Copy this file and customize for your environment
# DO NOT commit the actual config file with real credentials

# LDAP Server Configuration
LDAP_URL=ldap://your-ldap-server:389
# Alternative for secure connection:
# LDAP_URL=ldaps://your-ldap-server:636

# Base DN for LDAP searches
LDAP_BASE_DN=dc=example,dc=com

# Service account for LDAP binding
LDAP_BIND_DN=uid=service-account,ou=users,dc=example,dc=com

# TLS Configuration
LDAP_STARTTLS=true
LDAP_SERVER_NAME=your-ldap-server

# Connection timeout (optional, default: 10s)
LDAP_TIMEOUT=30s

# Authentication - Choose ONE method:

# Method 1: Direct password (not recommended for production)
# LDAP_PASSWORD=your-password-here

# Method 2: Password from file (recommended)
# LDAP_PASSWORD_FILE=/path/to/password/file

# Method 3: Use default paths (most secure)
# Place password in ~/.secrets/ldap/password
# The library will automatically find it

# TLS Certificate (optional)
# Method 1: Direct PEM content
# LDAP_CA_PEM="-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----"

# Method 2: Certificate from file
# LDAP_CA_PEM_FILE=/path/to/ca.pem

# Method 3: Use default paths
# Place certificate in ~/.secrets/ldap/ca.pem

# Testing Configuration (for integration tests)
TEST_LDAP_UID=test-user
TEST_LDAP_EMAIL=test-user@example.com

# Enable slow bulk tests (optional)
# RUN_BULK_TESTS=true

# Red Hat Specific Example Configuration
# =====================================
# For Red Hat employees, use these values:
#
# LDAP_URL=ldap://apps-ldap.corp.redhat.com:389
# LDAP_BASE_DN=dc=redhat,dc=com
# LDAP_BIND_DN=uid=pco-deleted-users-query,ou=users,dc=redhat,dc=com
# LDAP_STARTTLS=true
# LDAP_SERVER_NAME=apps-ldap.corp.redhat.com
#
# Then place your service account password in ~/.secrets/ldap/password

# Security Notes:
# ===============
# 1. Never commit files containing real passwords or certificates
# 2. Use environment variables or secret files in production
# 3. Restrict file permissions (600) for secret files
# 4. Use separate service accounts with minimal required permissions
